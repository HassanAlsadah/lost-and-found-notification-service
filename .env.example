# ============================================
# Factor 3: Configuration in env vars
# ============================================

PORT=3005
NODE_ENV=development

# Database (Factor 4)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=lostandfound
DB_USER=postgres
DB_PASSWORD=changeme

# Email provider (Factor 4: backing service)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password
EMAIL_FROM=noreply@lostandfound.com

# Logging (Factor 11)
LOG_LEVEL=info
